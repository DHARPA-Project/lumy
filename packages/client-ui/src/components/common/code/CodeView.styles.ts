import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  codeViewContainer: {
    padding: '0.5rem 1rem'
  },
  top: {
    display: 'flex',
    justifyContent: 'flex-end'
  }
}))
