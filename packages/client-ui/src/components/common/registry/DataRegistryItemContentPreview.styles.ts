import { makeStyles } from '@material-ui/core/styles'

export default makeStyles(() => ({
  missing: {
    height: '100px',
    width: '100px',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center'
  }
}))
