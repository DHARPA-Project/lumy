{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "MsgNotesUpdate",
  "description": "Update a note for a workflow step.",
  "properties": {
    "stepId": {
      "type": "string",
      "description": "Workflow step Id."
    },
    "note": {
      "$ref": "https://dharpa.org/schema/types/notes/Note.json"
    }
  },
  "required": [
    "stepId",
    "note"
  ]
}