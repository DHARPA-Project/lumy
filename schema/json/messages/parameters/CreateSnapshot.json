{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgParametersCreateSnapshot",
  "description": "Create snapshot of parameters of a step from the current workflow.",
  "additionalProperties": false,
  "properties": {
    "stepId": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    },
    "parameters": {
      "type": "object",
      "description": "Optional parameters of the step."
    }
  },
  "required": [
    "stepId",
    "parameters"
  ]
}