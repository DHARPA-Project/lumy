{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgParametersUpdated",
  "description": "Updated parameters of a step in the current workflow.",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    },
    "parameters": {
      "type": "object",
      "description": "Optional parameters of the step."
    }
  },
  "required": [
    "id"
  ]
}