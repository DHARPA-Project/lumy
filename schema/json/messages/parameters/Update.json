{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgParametersUpdate",
  "description": "Update parameters of a step in the current workflow.",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    },
    "parameters": {
      "type": "object",
      "description": "Optional parameters of the step that we are setting."
    }
  },
  "required": [
    "id"
  ]
}