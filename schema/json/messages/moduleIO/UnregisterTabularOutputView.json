{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgModuleIOUnregisterTabularOutputView",
  "description": "If there is a view of a table with the provided ID, unregister this view and stop sending updates about it to the frontend.",
  "additionalProperties": false,
  "properties": {
    "viewId": {
      "type": "string",
      "description": "An ID associated with this filtered version of the tabular value.\nThis is needed to distinguish between different views of the same data value that may exist independently."
    },
    "stepId": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that we are getting parameters for."
    },
    "outputId": {
      "type": "string",
      "description": "Unique ID of the output"
    }
  },
  "required": [
    "viewId",
    "stepId",
    "outputId"
  ]
}