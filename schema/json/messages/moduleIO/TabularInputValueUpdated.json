{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgModuleIOTabularInputValueUpdated",
  "description": "A filtered version of a tabular input of a step from the current workflow.",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that we are getting parameters for."
    },
    "inputId": {
      "type": "string",
      "description": "Unique ID of the input"
    },
    "filter": {
      "$ref": "https://dharpa.org/schema/types/data/TabularDataFilter.json"
    },
    "value": {
      "type": [
        "object",
        "string"
      ],
      "description": "The actual value payload. TODO: The type will be set later"
    }
  },
  "required": [
    "id",
    "inputId",
    "filter"
  ]
}