{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgModuleIOGetOutputValues",
  "description": "Get values of outputs of a step from the current workflow.",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that we are getting values for."
    },
    "outputIds": {
      "type": "array",
      "description": "Limit returned values only to outputs with these IDs.",
      "items": {
        "type": "string"
      }
    },
    "fullValueOutputIds": {
      "type": "array",
      "description": "Output IDs for which the full value should be returned. \nThis is only relevant for big complex types.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "id"
  ]
}