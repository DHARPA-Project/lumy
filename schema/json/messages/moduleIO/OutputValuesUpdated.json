{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgModuleIOOutputValuesUpdated",
  "description": "Output IDs of a step in the current workflow that had their values updated.",
  "additionalProperties": false,
  "properties": {
    "stepId": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    },
    "outputIds": {
      "type": "array",
      "description": "IDs of outputs that had their values updated.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "stepId",
    "outputIds"
  ]
}