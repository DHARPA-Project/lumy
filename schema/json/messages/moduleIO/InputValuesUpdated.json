{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "title": "MsgModuleIOInputValuesUpdated",
  "description": "Input IDs of a step in the current workflow that had their values updated.",
  "additionalProperties": false,
  "properties": {
    "stepId": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    },
    "inputIds": {
      "type": "array",
      "description": "IDs of inputs that had their values updated.",
      "items": {
        "type": "string"
      }
    }
  },
  "required": [
    "stepId",
    "inputIds"
  ]
}