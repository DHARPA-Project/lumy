{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "MsgModuleIOPreviewUpdated",
  "description": "Contains preview of I/O data of a step from the current workflow.",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that the preview is for."
    },
    "inputs": {
      "type": "object",
      "description": "Input data of the module. Key is input Id."
    },
    "outputs": {
      "type": "object",
      "description": "Output data of the module. Key is input Id."
    }
  },
  "required": [
    "id",
    "inputs",
    "outputs"
  ]
}