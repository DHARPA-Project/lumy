{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "MsgModuleIOPreviewUpdated",
  "description": "Contains preview of I/O data of a step from the current workflow.",
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that the preview is for."
    },
    "inputs": {
      "type": "array",
      "description": "Input data for the module"
    },
    "outputs": {
      "type": "array",
      "description": "Output data for the module"
    }
  },
  "required": [
    "id",
    "inputs",
    "outputs"
  ]
}