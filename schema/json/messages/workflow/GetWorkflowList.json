{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "MsgWorkflowGetWorkflowList",
  "description": "Request a list of workflows available for the user.",
  "properties": {
    "includeWorkflow": {
      "type": "boolean",
      "description": "If set to true, include workflow body."
    }
  },
  "required": [
  ]
}