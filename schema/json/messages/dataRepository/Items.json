{
  "$schema": "http://json-schema.org/schema",
  "type": "object",
  "additionalProperties": false,
  "title": "MsgDataRepositoryItems",
  "description": "Items from data repository.",
  "properties": {
    "items": {
      "description": "Serialized table with items as rows."
    },
    "stats": {
      "type": "object",
      "description": "Stats of the data repository."
    },
    "filter": {
      "$ref": "https://dharpa.org/schema/types/dataRepository/ItemsFilter.json"
    }
  },
  "required": [
    "items",
    "stats",
    "filter"
  ]
}