{
  "$schema": "http://json-schema.org/schema",
  "$id": "https://dharpa.org/schemas/vre/messages/moduleIO/MsgModuleIOExecute.json",
  "type": "object",
  "description": "Run this step with the latest used parameters on all data (not preview only).",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow."
    }
  },
  "required": [
    "id"
  ]
}