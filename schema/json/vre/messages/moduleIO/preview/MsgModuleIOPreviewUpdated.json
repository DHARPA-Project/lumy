{
  "$schema": "http://json-schema.org/schema",
  "$id": "https://dharpa.org/schemas/vre/messages/moduleIO/preview/MsgModuleIOPreviewUpdated.json",
  "type": "object",
  "description": "Contains preview of I/O data of a step from the current workflow.",
  "additionalProperties": false,
  "properties": {
    "id": {
      "type": "string",
      "description": "Unique ID of the step within the workflow that the preview is for."
    },
    "inputs": {
      "type": "array",
      "description": "Inputs data for the module"
    },
    "outputs": {
      "type": "array",
      "description": "Inputs data for the module"
    }
  },
  "required": [
    "id",
    "inputs",
    "outputs"
  ]
}