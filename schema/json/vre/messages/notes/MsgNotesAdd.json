{
  "$schema": "http://json-schema.org/schema",
  "$id": "https://dharpa.org/schemas/vre/messages/notes/MsgNotesAdd.json",
  "type": "object",
  "description": "Add a note for a workflow step.",
  "additionalProperties": false,
  "properties": {
    "stepId": {
      "type": "string",
      "description": "Workflow step Id."
    },
    "note": {
      "$ref": "https://dharpa.org/schemas/backend/Note.json"
    }
  },
  "required": [
    "stepId",
    "note"
  ]
}